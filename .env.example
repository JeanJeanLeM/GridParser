# Auth0 (required for /api/me and optional for frontend config)
# Create a Single Page Application at https://manage.auth0.com/
# Enable Google in Authentication > Social and Email in Authentication > Database
AUTH0_DOMAIN=your-tenant.auth0.com
AUTH0_CLIENT_ID=your_client_id

# Optional: Auth0 API identifier so access tokens are JWTs. If unset, /api/me validates ID tokens (use same value as AUTH0_CLIENT_ID in env).
# AUTH0_AUDIENCE=https://your-api-identifier

# For local dev: copy this file to .env and set the values. For Vercel: set these in Project > Settings > Environment Variables.
