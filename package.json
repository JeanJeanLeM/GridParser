{
  "name": "imageparser",
  "version": "1.0.0",
  "description": "Grid to Icon — split grid images into tiles in the browser",
  "private": true,
  "scripts": {
    "dev": "npx serve .",
    "local": "vercel dev",
    "test:grid": "node scripts/test-grid-split.js"
  },
  "dependencies": {
    "jimp": "^0.22.12"
  },
  "devDependencies": {
    "tesseract.js": "^5.1.0",
    "vercel": "^50.22.1"
  }
}
